{
  "_comment": [
    "Tests that fail today, and were already failing in the application repository",
    "before the Library moved here. CI refuses a change that adds to this list, and",
    "refuses one that fixes an entry without removing it — so the list can only",
    "shrink. See CLAUDE.md, 'Known failures', for what each group is waiting on.",
    "Regenerate with: npm run gate -- --update"
  ],
  "known": []
}
